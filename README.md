# Calculator App
A calculator built with React (frontend) + Flask (backend)

## Setup

### Backend
cd backend
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python app.py

### Frontend
cd frontend
npm install
npm start
